version: 1
frontend:
    phases:
        preBuild:
            commands:
                - cd frontend
                - npm install --include=dev
        build:
            commands:
                - npx react-scripts build
    artifacts:
        baseDirectory: frontend/build  # Change to where React is built
        files:
            - '**/*'
    cache:
        paths: []
